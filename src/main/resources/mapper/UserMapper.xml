<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.hc.spring.mapper.UserMapper">

    <resultMap id="UserResultMap" type="org.hc.spring.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="email" property="email" />
        <result column="created_time" property="createdTime" />
    </resultMap>

    <insert id="insertUser" parameterType="org.hc.spring.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, email)
        VALUES (#{username}, #{password}, #{email})
    </insert>

    <select id="selectByUsername" parameterType="String" resultMap="UserResultMap">
        SELECT id, username, password, email, created_time
        FROM users
        WHERE username = #{username}
    </select>

    <select id="selectByUsernameAndPassword" resultMap="UserResultMap">
        SELECT id, username, password, email, created_time
        FROM users
        WHERE username = #{username} AND password = #{password}
    </select>

</mapper>