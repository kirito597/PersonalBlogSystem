<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.hc.spring.mapper.CommentMapper">

    <resultMap id="CommentResultMap" type="org.hc.spring.entity.Comment">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="target_type" property="targetType" />
        <result column="target_id" property="targetId" />
        <result column="content" property="content" />
        <result column="parent_id" property="parentId" />
        <result column="status" property="status" />
        <result column="created_time" property="createdTime" />
        <result column="username" property="username" />
        <result column="level" property="level" />
        <result column="parent_username" property="parentUsername" />
    </resultMap>

    <insert id="insertComment" parameterType="org.hc.spring.entity.Comment"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comments (user_id, target_type, target_id, content, parent_id, status, level)
        VALUES (#{userId}, #{targetType}, #{targetId}, #{content}, #{parentId}, #{status}, #{level})
    </insert>

    <select id="selectByTarget" resultMap="CommentResultMap">
        SELECT c.*, u.username,
               (SELECT u2.username FROM comments c2
                                            LEFT JOIN users u2 ON c2.user_id = u2.id
                WHERE c2.id = c.parent_id) as parent_username
        FROM comments c
                 LEFT JOIN users u ON c.user_id = u.id
        WHERE c.target_type = #{targetType} AND c.target_id = #{targetId} AND c.status = 1
        ORDER BY
            CASE WHEN c.parent_id = 0 THEN c.id ELSE c.parent_id END,
            c.created_time ASC
    </select>

    <select id="selectById" parameterType="Integer" resultMap="CommentResultMap">
        SELECT c.*, u.username,
               (SELECT u2.username FROM comments c2
                                            LEFT JOIN users u2 ON c2.user_id = u2.id
                WHERE c2.id = c.parent_id) as parent_username
        FROM comments c
                 LEFT JOIN users u ON c.user_id = u.id
        WHERE c.id = #{id} AND c.status = 1
    </select>

    <select id="selectRepliesByParentId" resultMap="CommentResultMap">
        SELECT c.*, u.username,
               (SELECT u2.username FROM comments c2
                                            LEFT JOIN users u2 ON c2.user_id = u2.id
                WHERE c2.id = c.parent_id) as parent_username
        FROM comments c
                 LEFT JOIN users u ON c.user_id = u.id
        WHERE c.parent_id = #{parentId} AND c.status = 1
        ORDER BY c.created_time ASC
    </select>

    <select id="getReplyCount" resultType="Integer">
        SELECT COUNT(*) FROM comments
        WHERE parent_id = #{parentId} AND status = 1
    </select>

    <update id="deleteComment">
        UPDATE comments SET status = 2 WHERE id = #{id} AND user_id = #{userId}
    </update>

</mapper>